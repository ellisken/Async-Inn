@model AsyncInn.Models.Room

@{
    ViewData["Title"] = "Edit";
    Layout = "_Layout";
}
<h2>Edit</h2>

<h4>Room</h4>
<hr />
<div >
    <div >
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" ></div>
            <input type="hidden" asp-for="ID" />
            <div >
                <label asp-for="Name" ></label>
                <input asp-for="Name"  />
                <span asp-validation-for="Name" ></span>
            </div>
            <div >
                <label asp-for="Layout" ></label>
                <select asp-for="Layout" asp-items="@Html.GetEnumSelectList(typeof(AsyncInn.Models.Layout))"></select>
                <span asp-validation-for="Layout" ></span>
            </div>
            <div >
                <input type="submit" value="Save"  />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>